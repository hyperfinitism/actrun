name: e2e-unsupported-matrix
'on': push
jobs:
  build:
    strategy:
      matrix:
        os:
          - ubuntu-latest
    outputs:
      value: ${{ steps.emit.outputs.value }}
    runs-on: ubuntu-latest
    steps:
      - id: emit
        run: printf '%s=%s\n' value nope >> "$GITHUB_OUTPUT"
