name: secrets
'on': push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Token is ${{ secrets.API_TOKEN }}"
      # Run with: ACTRUN_SECRET_API_TOKEN=xxx actrun workflow run examples/05-secrets.yml
